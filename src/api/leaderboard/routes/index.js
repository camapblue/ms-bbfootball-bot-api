const update = require('./update');
const get = require('./get');
const reset = require('./reset');
const forceUpdatePoint = require('./forceUpdatePoint');

module.exports = [
  update,
  get,
  reset,
  forceUpdatePoint
];
